# 작업 규칙

## 파일 설명

- 임시 파일은 사용 후 삭제한다.
- 파일에 수정 사항이 생기면 일부만 수정하는 게 아니라, 처음부터 깔끔하게 다시 작성한다. 편집 흔적을 남기지 않는다.

## 기본 규칙

- claude code의 메모리 기능 쓰지 않는다.
- git 명령어는 사용하지 않는다.
- '~' 대신 '-'를 사용한다. md 문법 때문에 취소선으로 렌더링 된다.
- 모든 작업에 Todos 기능을 활용한다.
- 사용자의 허락 없이 파일을 생성하지 않는다.
    - 파일을 생성할 땐 파일 이름, 폴더 구조을 제안하고, 사용자 허락 후에 생성한다.
    - 임시 파일은 제외
- 모든 단계의 파일럿 테스트에는 시각화 체크를 포함한다.
    - 파일럿 테스트와 본 실행은 하나의 파일에서 실행한다. (파라미터로 파일럿 테스트 / 본 실행 결정)
    - 파일럿 테스트의 결과물은 check/ 에 저장한다. (data/는 사용 X)

### 코드 작성 유의 사항

- 각 클래스는 하나의 책임만 가진다. (SOLID의 단일 책임 원칙 준수)
- 속성은 private/protected 관례(_ 또는 __)를 사용해 캡슐화하고, 필요하면 getter/setter(@property)를 쓴다.
- 가급적 인터페이스나 추상 클래스(abc 모듈)를1 활용해 결합도를 낮춘다.
- 프로세스 실행 중 상태를 확인할 수 있도록 로그를 남기도록 코드를 작성한다. (최종 출력 로그 X, 실행 중 출력 로그 O)
- CPU 사용 최소화, GPU 사용을 최대화하도록 코드를 작성한다.
- 쓸데 없는 개행 넣지 않는다.
- 변수명은 변수가 어떤 기능을 하는지 알아볼 수 있도록 정한다.
- 주석 작성 규칙
    - 모든 클래스·함수에 docstring을 작성한다.
    - 논리 블록 단위로 주석을 작성한다.
    - 구현한 기능을 쉽고 간결하게 1줄로 설명한다.
    - 수식·기하 계산에는 유도 근거를 주석으로 남긴다. (어떤 정리·규약에서 나온 식인지, 변수의 물리적 의미)
    - 주석은 코드 위에 작성한다. (코드 옆에 작성 X)

### 코드 실행 유의 사항

- **호스트(로컬) 환경을 건드리지 않는다.**
- 사용자 요청 없이 코드를 직접 실행하지 않는다.
- 문제가 발생하면 코드 레벨에서 우회하지 않고, 문제가 되는 코드를 분석하여 직접적으로 해결한다.

## Docker 관련 규칙

- 컨테이너 이름은 `airlab_hw_{컨테이너 이름}`, 이미지 이름은 `t0mark251/{이미지 이름}`으로 작성한다.
- 도커 실행 및 빌드 관리는 compose를 사용한다.
- 초기 도커 파일 검증 방법 : 빌드는 최소한으로 진행
    1. Dockerfile 초안 작성
    2. 본 실행 및 컨테이너에서 수행할 작업 테스트
    3. 의존성 문제등 발생 시, 컨테이너 내부에서 해결 (Dockerfile 수정 X)
    4. 모든 의존성 문제 해결 완료 후, 도커 파일에 반영

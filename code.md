# 작업 파일 기록

> 2026-07-27 전면 리팩토링(사용자 지시) 반영판. 구조 변경 매핑은 문서 끝
> "리팩토링 기록" 절 참조. 형식: 파일별 기능/사용법/특이사항.

## scripts/datasets/URDF/ (common.py · templates.py · validate.py · generate.py)

- **기능**: 랜덤 URDF 생성 파이프라인 — 클래스별 형태 문법(humanoid/multileg 4·6·8족/wheeled 4종), 목표 외곽 치수 역산, pybullet 정적 검증(자기충돌·지지다각형·관성·치수), 센서(link+fixed joint) 부착.
- **사용법**: `docker exec airlab_hw_mansion bash -c 'cd /workspace/research/scripts/datasets/URDF && python generate.py --n 20 --seed 42 --out /tmp/urdf_pilot --check-dir /workspace/research/check/URDF'`. 본 풀 = `/data/URDF/{train,val_unseen_dims,test_unseen_form}` 480/60/60(홀드아웃 mecanum·hex, splits.json 매니페스트).
- **특이사항**: primitive 3종만(capsule 미지원)·관성은 해석식만. 파일럿 산출물은 data/ 금지(seed 재현). 링크 이름 prefix = GNN 노드 타입(NODE_TYPES — models/urdf_gnn.py와 동일 목록 유지 의무). 본 풀 통계: w_eff 0.12~1.00 / cam_h 0.09~1.81(5cm 버킷 35개).

## scripts/datasets/MANSION/ (common.py · 00_gates · 01_expert · 02_rendering · 03_topomap · 04_instructions · 05_generate_datasets)

- **기능**: MANSION 데이터셋 파이프라인 — 씬 GT 기하(콜라이더+벽 폴리곤) 게이트 추출·통과성 라벨 행렬, 관측 기반 expert 주행(drive_expert), 실높이 렌더(제안기 히트맵·heading GT), rule-based 토포맵(기억 그래프), LLM 지시 3형태, 통합 본생성(05).
- **사용법**: 각 스크립트 docstring 참조. 본생성 완료본 = `/data/EVLN_dataset`(4환경 300+ep).
- **plan 개정(2026-07-27) 반영분** — common.py가 규약의 단일 원천이고 번호 스크립트는 얇은 실행·카드 껍데기다:
  - `GATE_H_LEVELS=(0.15,0.6,1.1,1.6)` 4단 협착부 탐색 합집합(`GATE_MERGE_R=0.4` 내 중복은 병합, `h_hits`에 나머지 높이). 바닥 한 높이만 훑으면 가구 아래 통로의 높이 제약이 게이트로 안 잡힌다.
  - **`gate_label(geom, g, robot)` = 통과성 라벨의 단일 창구.** 기록된 폭이 아니라 `width_map(로봇 h)`에서 읽어 expert 주행 판정(`passable`)과 원천을 일치시킨다. 00_gates·05·drive_expert 전부 이 함수 사용.
  - `drive_expert(..., belief_h=BELIEF_H)` — 낙관 가정을 폭뿐 아니라 높이에도 적용. `belief_h=robot["h"]`를 주면 구판 재현(파일럿 전·후 비교 전용), `BuildingNav(floors, belief_h=)`로도 전달.
  - 제안기 GT: `reach_mask`(반경 침식 그리드의 현 위치 연결 성분, HEAT_MAX_M 창) → `heat_gt`(픽셀별 depth 리프트 → 바닥·reach·5m 판정 → 32² 0/1, 경계 1픽셀 `HEAT_IGNORE`) / `heat_pixel`(월드 바닥점 → 히트맵 픽셀, `project` 재사용).
  - 전역 선택 GT: `GSEL={"forward","node","stop"}` + `global_gt(pose, t, heat)` 3분기. 정답 월드 좌표는 `pose[t+1]`이 이미 갖고 있어 따로 저장하지 않는다.
  - `load_floor(building_dir, no)` = 한 층만 로드(GT 재유도 워커용), `_floor_scene`이 MANSION_VARIANT 처리 담당.
  - 상태 라벨 `impossible_pre` → **`impossible_noroute`**(이어진 길 자체가 없어 궤적 0프레임). `impossible_embodiment`(주행 후 포기, 궤적 있음)와 원인은 같고 차이는 궤적 유무뿐 — 옛 이름이 "미리 안다"로 읽혀 혼동을 유발했다. 사용처 = 05 `state_of`·pair 필터, models/eval.py `COLLAPSE`.
- **특이사항**(누적 함정 기록 유지): 개구부 도려내기는 법선 ±0.13m 방식(cv2.line 캡 버그), 계단실·엘베 방 내부는 free_mask 제외, 경로 비용 클리어런스 가중, `natural_path()` 스무딩만 허용, **AI2-THOR 기동 = `launch_controller(render=)` 2모드**(CloudRendering GPU / 패치 빌드 상호작용, 훅 없으면 빈 방), 문 규칙 = 문짝 제거(Doorframe 교체)·엘베 문 커스텀 에셋, `sim_view()` 정사영 탑뷰+벽 오버레이, GT 트래젝토리 = 관측 기반(라이다 360° 가정 금지), 계단 = 물리 등반 폴리라인(sb/sa 앵커 분리), legged 수단 선택 = 규칙(비용 아님), medial_axis rng 고정.
- **05 본생성 오케스트레이션(2026-07-27 재생성용 개정)**: 웨이브가 `--skip`을 gates·combined_eps·evalsets에만 적용해 episodes 단계를 못 건너뛰었고 `gt_rederive`는 웨이브에 아예 없었다 → **episodes_plan·episodes_render·instructions·topomap까지 skip 적용 + gt_rederive를 W1에 병치**(무렌더라 gates 렌더와 겹쳐도 무방). 웨이브별 누적 시간 로그 추가. `--only gt_rederive`가 KeyError였던 `fns` 누락도 수정. gt_rederive 워커는 32개 상한(층 기하를 워커마다 들고 있어 코어 수만큼 띄우면 메모리가 샌다).
- **05 스테이지가 03·04 개정을 안 물려받던 결함 2건**(본생성 1회차 산출물 검수에서 발견): ① `stage_instructions`에 hard prompt 로직이 없어 목표형 70건이 전부 평범한 지시로 생성됨 → `m04.HARD_KINDS`·`GEN_HARD_SYS`·`ordinal_conflict`·`GEN_RETRY` 이식, `SCALE` 3종에 `hard_ratio=0.2` 추가 ② `stage_topomap`이 `object_coverage`를 호출하지 않아 json `coverage`가 비어 있었음 → 호출·기록·로그 추가. **03의 `GraphBuilder`는 재사용하므로 앵커 스캔 제거는 이미 반영돼 있었다** — 05가 03·04 함수를 부르는 부분과 자체 구현한 부분을 구분해서 볼 것.
- **본생성 재생성(2026-07-27, plan 재생성 범위표대로)**: 주행 에피소드 927개 렌더는 **재사용**(`--skip episodes_plan episodes_plan_merge episodes_render`), GT는 `gt_rederive`로 오프라인 재유도, 게이트·기억·지시·평가셋만 재생성. 4환경 합계 = 게이트 593 / 지시 352(hard 35) / zswap 636 · conflict 378 · notfound 25 / 결합형 60, 데이터셋 10 GB. 역파싱 351/352(실패 1건은 결합형 "bed" 중의성, `pass=False`로 하류에서 걸러짐), 서수 모순 0, 에피소드 927개 전부 GT 필드 9종 완비, 전역 분기 forward 53%·node 38%·stop 9%.
  - **게이트 샤드 수는 반드시 기존과 같게 둘 것 (조용한 손상 원인)**: 소비처(loaders·eval)가 `{tag}_snaps_shard*.npz`를 **glob으로 전부 읽으므로**, 샤드 수를 8→4로 줄이면 구 샤드 4~7이 남아 함께 읽힌다. 그 안의 `key=(gi,…)`는 옛 게이트 목록 기준이라 이미지-라벨이 어긋난다(용량 낭비가 아니라 정합 손상). `--gate-shards`로 기존 수에 맞춰 전량 덮어쓰는 것이 정답 — 삭제 없이 해결된다. 호텔 1회차를 4샤드로 돌렸다가 이 문제로 8샤드 재렌더함.
  - 렌더 속도: 4샤드 45분 → **8샤드 20~27분**(렌더당 0.34~0.40 s, GPU당 2샤드). 동시 CreateHouse 타임아웃은 8샤드에서도 재시도로 자체 복구(1회 관측).
  - 환경별 특성: 오피스는 1.1 m 이상에서 잡힌 게이트가 38개로 호텔(19)의 2배 — 높이 변별은 씬 구조에 좌우된다. 주택은 계단만 있는 건물이라 **wheeled 토포맵이 1층 4노드에 그친다**(2·3층 전부 `no_route_meta`) — 층 전환 매트릭스의 "계단만 → wheeled 불가능" 칸이 의도대로 재현된 것.
- **메타 플래너는 전지적이다(의도된 현행)**: `_route`가 로봇의 진짜 침식 그리드를 쓰므로 "길 없음"은 주행 전에 결정된다. 관측 기반 판정은 층 내부 구간(`drive_expert`)에만 적용. 그 결과 "주행 후 포기" 궤적은 같은 층에서만 생기며(실측 40건 전부 same-floor), 05·02가 **불가능 쿼터 탐색**(폭 내림차순으로 판정 유도)으로 표본을 확보한다. 구조 변경은 검토 후 불채택 — 불가능 판정이 학습이 아니라 규칙 모듈이라 실질 손해가 없다.

## scripts/figure/ (fig1_system_overview.py · fig1_compose.py · urdf_render.py)

- **기능**: 논문 Fig 1 생성 — 같은 지시를 받은 두 로봇이 embodiment 때문에 계단·엘베로 갈라지는 한 장짜리 그림. 산출물 = `check/figure/fig1_system_overview.{png,pdf}`.
- **사용법**: `docker exec airlab_hw_mansion bash -c 'cd /workspace/research/scripts/figure && python fig1_system_overview.py --stage {render|plan|compose}'` (render는 `-e MANSION_GPU=`). 3단계 분리 = render(GPU·AI2-THOR, 캐시 1회) / plan(플래너만) / compose(캐시만, 반복 수정이 싸다). 캐시 `check/figure/_cache/`.
- **그림의 근거는 연출이 아니라 플래너 출력**: 오피스 1층 리셉션 → 3층 팬트리로 같은 목표를 주면 `turtlebot3_waffle`(stairs_ok=False)는 `transitions=[elevator]` 비용 36.9, `unitree_go2`(True)는 `[stairs, stairs]` 32.8. 앞 322셀은 공유하고 분기점에서 갈린다.
- **카메라 = 복도에서 낮게 올려다보는 시점**: `(8.6, 2.35, 9.0)` → `(7.0, 2.90, 0.8)`, fov 64, 부각 **-3.8°(위로)**. 높이 2.35m는 임의값이 아니라 하한이다 — 라운지 가구가 z 4~7을 채우고 로봇은 z=2.5·높이 0.3m라, 2.0m 아래로 내리면 시선이 의자 등받이에 걸려 로봇째 가린다(1.7m·거리 6m에서 가구 지점 시선 높이 0.9m). 불채택 이력: 대각선 BEV(부각 35~48°)는 계단 디딤판이 눌림 / 화장실 안(x<5)은 복도 벽이 시야를 막아 경로가 벽 위에 그려짐 / 높이 0.85~1.45m는 가구에 파묻힘 / z=5.2로 당겨 가구를 등 뒤로 빼면 문이 화면을 채우고 로봇이 잘림. **"올려다보기"와 "바닥 경로 보여주기"는 근본적으로 상충**한다 — 앙각이 커질수록 바닥이 압축돼 두 경로가 겹친다.
- **옆벽 재질 평탄화** `plain_side_wall()`: 화장실 경계벽(x=5)의 파란 세라믹 타일이 화면 1/4을 먹으며 시선을 끌었다. **벽 제거는 두 안 모두 불채택** — 전부 지우면 그 벽에 달린 문이 참조를 잃어 `CreateHouse`가 `Sequence contains no matching element`로 죽고, 화장실 쪽 면만 지우면 변기·칸막이가 드러나 더 산만해진다. 기하는 두고 재질만 `LightWhite`로 바꾼다.
- **가려진 경로는 지우지 말고 점선으로** `split_runs()`: 가림 판정이 경로를 토막 내는데 짧은 조각을 버리면 **이미 주행한 길이 실제보다 훨씬 짧아 보인다**(사용자 지적). 보임=굵은 실선+흰 테두리, 가림=가는 점선(도면의 은선 규약). MANSION 층은 위에서 보면 천장이 없지만 **실내 높이에 두면 천장이 보인다**(한쪽 면 렌더). `ceilingMaterial`을 None으로 바꾸면 CreateHouse가 NullReference로 죽으니 씬은 원본 그대로 로드할 것.
- **월드→픽셀**은 Unity 오일러(roll 0) 기저로 직접 계산하고 렌더 깊이와 대조해 검증한다. **부각이 낮을수록 검증 지표가 나빠진다**(35°에서 중앙 5.8cm → 13.8°에서 81cm) — 투영이 틀린 게 아니라 표본점이 가구 표면에 얹혀서다. 판단은 지표가 아니라 경로가 바닥에 붙었는지로 할 것.
- **가림 판정** `visible_runs()`: 렌더 깊이와 점까지 거리를 비교해 벽·가구 뒤 구간을 끊는다. 렌더 깊이는 **20m에서 잘리므로**(실측 max 19.99) 하늘·원거리 판정에는 못 쓴다.
- **화면 자르기는 성긴 기준점으로**(`roi_box`): 두 문(바닥~2.75m)·두 로봇 자리·분기점 **6~9개 점만** 쓴다. 함정 두 가지를 다 밟아봤다 — ① 고정 월드 상자로 자르면 카메라 옆 벽이 화면 1/3을 먹는다(낮은 시점일수록 가까운 면의 화각 점유가 크다) ② **조밀한 경로점을 넣으면 못 쓴다**: 카메라 코앞 구간이 원근 때문에 화면 밖 수천 픽셀로 투영돼(실측 v 최대 12085, 이미지 높이 1400) 최소·최대는 물론 백분위(1.5/98.5)로도 안 걸러지고, 화면 안 점만 남기면 이번엔 밀집한 근거리 점이 표본을 지배해 로봇에 확대돼 버린다. 주행 구간은 상자를 **아래로만 55% 늘려** 담는다. 가로비 하한 1.52, 여백 13%, 결과는 이미지 경계로 클램프 필수 — 안 하면 이미지 밖 여백에 얹힌 주석이 그림 밖으로 밀려난다.
- **로봇 배치** `stand_point()`: 경로의 마지막 점은 계단·엘베 **방 안의 전환 앵커**다(MANSION은 두 방 내부를 free_mask에서 제외). 거기 세우면 문턱을 넘어 방 안에 서 있게 되므로 **방 경계 z=2 바깥(ROOM_FRONT_Z=2.5)에 남는 마지막 점**을 쓴다. 거리로 되짚는 방식(0.95m)은 방 깊이가 제각각이라 여전히 안쪽에 걸렸다.
- **로봇 그림 = 공개 description 패키지 메시**(`scripts/figure/assets/`, `research_assets/robot_urdfs_src`에서 복사, 90MB). **`/data/URDF/real_robots`의 URDF는 visual이 `package://none/none.stl` 자리표시자**라 렌더하면 TurtleBot이 상자로 보인다. `urdf_render.py`가 `package://`·`${namespace}`를 치환해 pybullet DIRECT로 투명배경 RGBA를 뽑고 픽셀당 미터를 함께 돌려줘 씬 원근 배율에 맞춰 합성한다. **로봇 방향은 고정값이 아니라 계산한다**(`facing_yaw`) — 실측 규약은 pybullet yaw 270이 카메라 정반대(등이 보임)·90이 정면·0이 화면 오른쪽이므로 `270 + (진행방향_월드 - 카메라_방위)`. 고정값으로 두면 카메라를 옮길 때마다 어긋난다(실제로 245로 박아뒀다가 시점 변경 후 방향이 틀어졌다). 치수 대조(`check_dims`): TurtleBot 메시 0.272×0.310×0.146 vs 풀 0.281×0.306×0.141로 1cm 내 일치, Go2는 0.582×0.330×0.507 vs 0.700×0.310×0.400 — **메시 AABB는 조인트 0도, 풀 값은 기립 자세 기준**이라 l·h가 다르다. 판정에 쓰이는 폭은 양쪽 0.31로 일치.
- **특이사항**: `research_assets`는 컨테이너에 마운트되지 않는다(`research`·`data`만) — 컨테이너에서 쓸 자산은 `research/` 아래로 복사할 것. 말풍선·범례는 **데이터 좌표**로 배치한다(축 비율 좌표는 imshow가 화면비 맞추며 남긴 여백에 얹혀 이미지 밖으로 나감). 말풍선 문구의 "kitchen"은 실제 목표 방이 `pantry area`(냉장고·아일랜드 카운터 보유) — 데이터셋 지시 어휘와 엄밀히 맞추려면 "pantry"로 바꿀 것. **폐기된 요소**: 우측 URDF 키네마틱 그래프 패널(사용자 불채택), 경로 색 견본 선 범례(이름 색으로 충분), 말풍선의 장면 가로지르는 지시선(꼬리까지 단일 폴리곤으로 교체 — 사각형과 삼각형을 따로 그리면 경계선이 남아 분리돼 보인다).

## check/MANSION/00_gates · 01_expert (P-1·P-2 파일럿, 2026-07-27)

- **00_gates**: 층별 멀티프로세싱(진행 로그·ETA), 단일 높이 vs 4단을 나란히 산출. 카드 = 게이트 색 = 최초 발견 높이 + 범례, json에 `block_rate_by_height`·`diag_height_sweep`·`cause_h_vs_w`·`clearance_hist`.
  - **실측(호텔 4층)**: 게이트 105→185개. 폭 0.3m 고정 키 스윕 차단율 = 단일 42/46/53/53/53/53% → 4단 49/62/70/71/71/71%(h=0.4~1.9m). **0.4~1.0m 구간은 살아났으나 1.0m 위는 여전히 평평** — 게이트 셀 클리어런스가 1.3m 미만 8%·3.0m 미만 9%로, 씬에 1.3~3.0m 대역 장애물이 없다. 추출 방식으로는 더 못 메우며 **씬 편집은 불채택(사용자 확정)** → embodiment 차별화는 폭·계단 중심.
- **05 `--only gt_rederive --dry-run [--limit N] [--card-dir D --n-cards K]`** (P-3·P-6 측정 모드): 기존 npz를 **쓰지 않고** 제안기 히트맵·전역 GT만 계산해 통계를 낸다. GT 규칙을 바꿀 때는 항상 이걸로 먼저 재고 덮어쓸 것. `fns`에 gt_rederive가 빠져 있어 `--only`가 KeyError였던 것도 같이 수정.
  - **실측(호텔 560에피소드 6,005프레임, 15초/워커 다중)**: 전역 분기 forward 59% / node 32% / stop 9%. 제안기 양성 비율 중앙값 20.4%(사분위 4.8~34.1%, 50% 초과 프레임 0) → **HEAT_MAX_M=5.0 유지**(포화·붕괴 없음). node 내역 = offscreen 94%(정상 방향 전환) / zone 6%(계단·엘베 존 진출입, 존은 free_mask 제외라 정답이 원래 노드) / **other 3건(0%)**. `other`는 두 GT가 어긋난 결함 신호이므로 0 근처를 유지해야 하며, 규칙 변경 시 이 값부터 볼 것.
  - **`_gt_card()`(P-6 검수 카드)**: 저장된 rgb·depth·pose만으로 프레임별 히트맵 오버레이(초록=양성/회색=경계무시) + 전역 분기 + forward 정답 십자 + **도착 heading 화살표**를 그리고, 머리말에 **에피소드 지시문**을 얹는다(지시가 뭐였는지 없으면 GT만으로 검수 불가). 렌더·시뮬 불필요. 엘베 문·벽 정면 프레임은 pos=0%·node로 나오는 것이 정상.
  - **도착 heading은 홉당 1개 (plan 2026-07-27 재확정 — 픽셀 dense 폐기)**: `common.global_head_gt(pose, t)` = `pose[t+1]`의 실제 heading(deg). 저장 = `z["gsel_head"]`(f16, 없으면 −999). 예측 주체는 제안기가 아니라 선택기이므로 제안기 손실은 heat BCE 단일이고 heading aux는 없다. **폐기된 시도 2건**(되돌리지 말 것): ① expert waypoint 1픽셀만 감독 → 양성 픽셀 수백 개가 무감독 ② 양성 픽셀 dense + 레이캐스트 개활 방향 기본값 → 목표를 안 보는 기하 대리물이라 열린 방·T자 갈림·엘베/goal 접근에서 틀림. 근본 원인은 제안기 입력이 depth뿐이라 학습 가능한 타깃이 기하밖에 없다는 것.
    - **홉별 heading 변화(plan P7-2 측정 항목)**: 중앙값 11.1° / 90퍼센타일 49.5° / **±45°(FOV 절반) 초과 12.4%**, 초과 홉 중 **그 방향에 기억 노드 있음 87%** → 노드 선택으로 돌 수 있으므로 홉 간격 축소는 불필요. 판정은 새 heading ±45°·8 m 안의 topomap 노드 존재 여부.
    - 카드 색 규약(02·05 공통): 초록=도달가능 / 회색=경계 무시 / **흰 십자=전역 선택 정답 픽셀** / **주황 화살표=도착 heading**. 프레임 안의 노란색은 폐기(구 단일 waypoint 시각화) — 탑뷰의 판정 지점 다이아몬드만 노랑 유지.
  - **`common.draw_heading()`** = 도착 heading 화살표 공통 규약(02·05 공용, `project` 재사용). 02의 화살표가 GT 개정 과정에서 빠져 있던 것을 이 함수로 복구 — heading GT(waypoint +0.8m look-ahead)는 보조 출력으로 유지되므로 카드에도 계속 표시한다.
  - 그 과정에서 고친 GT 정합 결함 2건: ① `global_gt`가 경계 무시 픽셀을 방향 전환으로 오라벨(277건 → forward로 교정) ② `reach_mask` 연결 판정 창이 좁아 모퉁이 우회 경로가 다른 성분으로 갈림 → `REACH_WIN_M=2×HEAT_MAX_M`, 포즈 스냅 한도 `REACH_SNAP_M=1.0m`(렌더 포즈는 스무딩·재표집으로 침식 그리드에서 살짝 벗어남).
- **03_topomap**(P-4): 앵커 360° 스캔 제거 — `GraphBuilder(anchor_scan=False)`가 기본이고 True는 구 규약 재현(비교 전용). 신규 `object_coverage()` = 노드·저장 heading에서 FOV 90°·10m·LOS로 가구 관측률 산출(04 결합형 goal 판정과 같은 규격), json에 `coverage`·`coverage_legacy_scan` 기록.
  - **실측(호텔)**: wheeled_diff_060 가구 213/802(27%) vs 구 스캔 300/802(37%), 방 단위 40/47 vs 43/47, 스냅샷 212 vs 301장 / multileg_quad_012 73/802(9%) vs 101/802(13%), 방 25/47 vs 33/47, 스냅샷 138 vs 204장. **커버리지는 상대 27~31% 감소하지만 관측 가능 대상 절대량(213·73개)이 goal 표집에 충분**하고 렌더 비용은 30% 줄어든다. multileg의 낮은 값은 스캔 때문이 아니라 폭 0.60m로 객실 11곳을 못 들어가는 embodiment 효과.
- **04_instructions**(P-5): `observed_gids(floors, registry, nodes, cam_h)` 신설 = 기억 노드·heading에서 FOV·LOS로 관측된 {gid: 최초 관측 노드}. **목표형·R2R·결합형 goal 전부 이 결과에서만 표집**(`sample_goals(observed=)`) — 결합형에만 있던 규칙을 전 형태로 통일, 결합형의 중복 구현도 이 함수로 흡수. R2R 갈림 쌍은 두 로봇 공통 관측 교집합에서. `--hard-ratio`(기본 0.2)로 어려운 표현 3종(negation·ordinal·conditional) 생성, 레코드에 `hard` 필드.
  - **실측(호텔)**: 목표 후보 381개(유일 63) → 관측 한정 wheeled 86(유일 19)·multileg 39(유일 11)·공통 29, 프리픽스50% 13·6. 역파싱 10/10 통과. **goal 풀이 유일 63 → 19/11로 줄어든다** — 파일럿엔 충분하나 본생성에서 에피소드당 goal 재사용이 잦아질 수치이므로 규모 산정 시 확인할 것.
  - **어려운 표현 3종 = negation·relational·conditional.** ordinal은 폐기 — goal의 `unique` 정의가 "그 층에 같은 이름 하나뿐"이라 "두 번째 X"류 서수는 **구조적으로 참일 수 없고**, 재생성 2회를 돌려도 같은 거짓 서수가 반복됐다. 대신 relational(같은 방의 다른 오브젝트를 경유한 간접 지시)을 난이도 축으로 쓴다.
  - **`ordinal_conflict()` = 거짓 서수 상시 검출기(잔존)**: 어떤 지시든 경로 의존 서수가 있는데 같은 이름 대상이 그 층에 하나뿐이면 폐기·재생성(`GEN_RETRY=2`, 역파싱 불일치와 같은 경로). 층 서수는 `(?![-\s]*floor)`로 제외 — 하이픈("second-floor")을 놓쳐 오탐이 났던 것을 수정.
  - **실측**: 규모 확대(요청 30 → 실제 19) 시 goal 17/19·r2r 6/6·결합형 8/8, 모순 1건(ordinal). ordinal 교체 후 축소 재실행 **10/10 통과·모순 0**. 요청 30건에 19건만 나온 것은 관측∩유일 goal 풀이 19개여서 — 본생성 규모 산정 시 이 상한부터 볼 것.
- **01_expert**: belief 높이 규약 전·후 비교 파일럿. 에피소드별 멀티프로세싱, 카드에 before(회색)·after(색) 경로 겹쳐 그리고 `[CHANGED:이유]` 표기.
  - **실측**: 72경로(18에피소드×대표 4대) 중 **변화 0건** — 성공 60→60, 관측 분기 2→2. belief 높이 수정은 규약을 바로잡지만 경로에는 영향이 없다 → **주행 에피소드 RGB-D 렌더 100% 재사용 확정**.
  - 실패 12건 = `no_route_meta` 10(길 없음) + `observed_unreachable` 2(주행 후 포기). 실패율이 로봇 폭에 단조 비례(0.30m→1/18 … 1.00m→4/18)해 embodiment 원인임이 확인됨.

## scripts/models/loaders.py (구 data/loaders.py — data/는 심볼릭 앵커만)

- **기능**: 본생성 데이터셋 PyTorch 로더 — `TraversabilityDataset`(loss a: 게이트 스냅샷×로봇→soft 라벨, ShardBatchSampler로 LRU 유지) / `EpisodeFrameDataset`(프레임 단위: heat 32² + 전역 라벨 gsel/gsel_uv/gsel_head + **depth 히스토리 K장**(모자라면 최오래 프레임 복제 = 모델 패딩 규약과 동일), 지시 결합 3형태. 구 스키마(heat 없음) 궤적은 `skipped_legacy`로 세고 건너뜀) / `RobotGraphCache`(URDF 그래프 캐시, collate에 augment_rng 넘기면 구조 증강) / evalsets·topomap 패스스루.
- **사용법**: scripts/models 경로에서 `import loaders`. 데이터 경로는 research/data의 심볼릭 링크(EVLN_dataset·URDF)를 앵커로 해석 — 절대 경로 금지. data/ 폴더에는 링크만 남김.
- **특이사항**: wp_bin==-1 프레임 제외, 지시 커버리지 ~60%(빈 문자열 반환), 거리빈 재계산 `_redist_bin`(N_DISTS=14).

## models/ — EA_Nav.py(최상위) + weights.py(로드 유틸) + modules/(태스크 모듈) + backbones/(순수 백본) + assets/

파일명 규약(2026-07-27 사용자 확정): 클래스 파일은 CapWords, 유틸(weights)만 소문자. 폴더 규약(사용자 확정): **backbones/ = 백본 원본(무수정 이식) — DepthResNet·CrossModalTransformer. 수정·태스크화는 modules/의 래퍼(상속)에서만, 백본 파일 본문 수정 금지.** 태스크 개념이 이름에 붙는 부품(waypoint TRM)은 백본이 아니라 해당 모듈에 병합.

- **modules/URDFEncoder.py**(구 urdf_gnn): URDF→그래프(NODE_DIM 27·EDGE_DIM 19)→z(128)+프로브(wlh·stairs_ok·cam_h). `urdf_to_graph(canonical=True 기본)` = fixed 링크 병합 정규화(운동학 불변, 실기 Go2 42→19노드), `augment_graph` = 학습용 더미 링크 증강(라벨 보존). NODE_TYPES는 생성기와 동일 목록 유지.
- **modules/VisionEncoder.py**(구 encoders — 자연어 아님, 관측 전용): DepthEncoder(DD-PPO 사전학습 ResNet50+gibson ckpt, 10m 정규화 내장 — 중복 정규화 금지; (B,H,W) 단일 프레임과 (B,K,H,W) 히스토리 스택 모두 수용), CLIPEncoder(ViT-B/32 512d + encode_text), **MonoDepthEstimator**(plan 기하 하이브리드 — depth 센서 미부착 로봇 분기. 프로즌 Depth-Anything-V2-Small 지연 로드 → 상대 역깊이를 지면 평면 모델(피치 0·카메라 높이)에 최소제곱 정합해 metric 환산. 실프레임 대조 AbsRel 0.268·δ<1.25 0.763 = 폴백 수준).

- **modules/TraversabilityHead.py**(구 traversability ← pass_head + film 흡수): z 주입 메커니즘 일체 — `TraversabilityHead` g(후보 feature 768, z; loss (a) 직접 학습 후 **네 소비처가 같은 인스턴스 공유** = 제안기 픽셀 맵·전역 선택기 고스트·기억 스냅샷 랭킹) + `FiLM`(ablation 전용). **`bias_logits(logits, feat, z, tau=, mask=)`** = 후보 로짓에 통과성을 얹는 공통 규약(가산 바이어스 + tau 미만 −inf 차단 + mask로 적용 대상 한정) — 픽셀/노드 어느 후보 축에도 동일 사용.
- **modules/WaypointNet.py**(구 waypoint_net + 구 backbones/WaypointBert 병합 — TRM 몸통은 백본이 아니라 이 넷 전용 부품이라 흡수, "# ==== 병합" 마커·클래스/서브모듈명 보존=가중치 이름 매칭): ForwardWaypointNet = **후보 제안기**. **출력 = 픽셀 히트맵**: `heat` (B,1,32,32) 로짓 + `feat` (B,768,32,32) 후보 feature 맵. **heading은 예측하지 않음**(2026-07-27 plan 재확정 — 제안기 입력이 depth뿐이라 학습 타깃이 기하 대리물뿐이고 복도에서만 맞음 → 선택기로 이관). 손실 = heat BCE 단일. 구조 = TRM 현재프레임 16토큰 → 업샘플 디코더 + **원본 depth 스템**(4×4 병목만으로는 자유공간 경계 해상도 부족) → feature 맵 → heat 헤드. feature가 768인 이유는 g가 노드·스냅샷과 같은 공간에서 동작해야 해서. inject: g(기본, 픽셀별 가산)/film/both/none. **후보 규약**(plan 확정): `propose()`가 시그모이드 **임계 0.5** 통과 셀을 점수 내림차순으로 내고, **월드 0.5m 억제 + 상위 K=5**는 depth가 필요해 리프트 단계(EANav.lift)가 수행. 임계 통과 셀이 없으면 고스트 0개가 정상 — 억지 fallback 금지(막다른 곳에서 노드+STOP 백트래킹이 유도돼야 함). **이미지 히스토리**: (B,K,128,4,4) 스택(K≤4, 인덱스 0=현재), zero-init frame_embed는 프레임 순서 신호만 없앨 뿐 히스토리 자체는 어텐션으로 반영(K=1 대비 수치오차 4.9e-04, 학습 후 2.7e-02). **구 ckpt 전면 비호환 — 재학습 필수**(load_compat이 차단).
- **modules/GraphPlanning.py**(구 nav_bert): ZNavBert — backbones/CrossModalTransformer(GlocalTextPathNavCMT) 래핑(백본 무수정, 수정은 전부 이 래퍼). 역할 3개 = 지시 인코딩(forward_txt) + 관측→노드 임베딩(forward_panorama K=1) + cross-modal 전역 선택기(forward_navigation), embodiment 토큰(워밍업 게이트, 기본 닫힘), rank_snapshots = g 직접. transformers 4.44 필요. **GraphMap**(전역 선택 후보 생산측 — ETPNav graph_utils.GraphMap+_nav_gmap_variable의 전방 단일 뷰·SE(2) 개조 이식): 방문 노드/고스트 생성·병합, front 경유 그래프 거리. **고스트 소멸 = 호출부가 선택 고스트를 delete_ghost(id)로 명시 삭제**(ETPNav 트레이너 계약 동일 — 도착 위치 최근접 자동 소멸은 실주행 도착 오차에서 오소멸/누락이 가능해 폐기), `gmap_inputs()`가 forward_navigation 입력(gmap_*·node_flags/types/floor_diff) 일괄 조립. **반경 2종**: node_radius 1.0m(이 안의 후보는 노드와 중복이라 고스트를 만들지 않고 엣지만 연결) / ghost_radius 0.5m(고스트 병합 = 제안기 NMS 억제 반경과 동일). **후보 집합 제한**: 현재 층 방문 노드 전부 + 다른 층은 전환 노드(계단·엘베)만 + 현재 뷰 고스트 + STOP, 총 **상한 128**(초과 시 현재 위치 근접순) — 다층 기억이 커져도 attention 비용 상수. 노드마다 floor 기록. 에피소드당 인스턴스 1개·B=1, 후보 좌표 규약 = EA_Nav.pixel_to_pose·robot_to_world 동일, networkx 의존. **`persistent_nodes()`** = 영속 기억(토폴로지 맵)에 내보낼 방문 노드만 반환 — 고스트는 미방문 자유공간이라 잠재 목적지 레지스트리에 기록 금지(plan 규칙), 기억 기록은 반드시 이 함수 경유. **전역 선택 학습(plan Phase 7)**: `node_flag_embed`(0=STOP/1=방문/2=고스트(미방문 후보) — 관측/고스트 플래그 통합·용어는 고스트로 통일, 구분은 feature 소관)·`node_type_embed`(0=일반/1=계단/2=엘베) zero-init — forward_navigation(node_flags=, node_types=, mask_visited=)로 가산, 미전달 시 원본 동작 불변. 백트래킹 학습·추론은 mask_visited=False. **g의 고스트 적용**: `g_bias=True`면 통과성 바이어스를 **고스트 후보에만** 얹고(방문 노드는 불개입 — 이미 지나온 곳), `g_tau`(벤치 τ=3.0)를 주면 미만은 −inf 차단. **STOP(인덱스 0)은 어떤 경로로도 마스킹되지 않음** = 도착 판단의 유일한 출구. **도착 heading = 선택기 예측**(2026-07-27 제안기에서 이관): `heading_head`가 후보 토큰 → sin·cos, forward_navigation이 (B,N,2) 단위벡터로 반환하며 **선택된 후보 위치의 값만** 사용(손실 1−cos(Δθ)도 정답 후보에서만). GT = `pose[t+1]`의 실제 heading. **층 차이**: `floor_diff_embed`(zero-init, ±4 클램프)를 노드 임베딩에 가산 — pos_fts 폭을 늘리면 백본 사영 가중치를 못 쓰므로 별도 임베딩 경로. **무지시 토큰**: `null_instruction(B, device)` = 학습 가능 파라미터 1토큰(지시 없는 프레임 ~40%를 버리지 않고 무조건부 주행으로 학습).
- **EA_Nav.py**(구 policy.py — 사용자 개명, 하이픈은 파이썬 임포트 불가라 언더스코어): `class EANav` 최상위 모델. mode 디스패치 = embodiment/language/observe/**propose**/navigation/rank_memory. **투영 규약**(데이터 생성부와 동일, 변경 시 양쪽 동시 수정): 256²·HFOV 90°→fx=fy=128·주점(128,128)·pitch 0·**depth = planar z-depth**. `cell_pixel(u,v)` = 히트맵 셀 → **셀 중심 렌더 픽셀 `u*8+4`**(GT common.heat_gt와 동일 표본 — 중앙값 풀링 아님), `pixel_to_pose`/`robot_to_world` = 리프트·역변환. GT 수식 대비 편차 8.9e-16(동일식), 데이터 project() 왕복 평균 8.2mm. 구 `candidate_to_pose`는 폐기. `lift(cand, obs, pose, floor_y, top_k=5, nms_radius=0.5, ground_tol=0.12, max_dist=5.0)` = 셀 중심 depth 표본 → 리프트 → **바닥 검사(|높이|≤0.12m, y=0 기준 — GT와 동일)** → 5m 이내 → 월드 0.5m 억제 → K=5. 반환 embed는 **채택 셀의 디코더 feature**(뷰 전체 임베딩 공유 금지 — 고스트끼리 구분 불가해짐). `observe(rgb, depth=None, cam_h=)` = depth 미전달이면 단안 추정 분기, 히스토리 버퍼 갱신(**기본 K=4**), depth_m/depth_feats/depth_hist/rgb_feat/node_embed 반환. **`reset_episode()`는 에피소드·씬 전환마다 필수**. `freeze_stage1()` = 1단계 이후 URDF GNN·g 동결(g는 네 소비처 공유라 손상이 연쇄). 상태 집합·홉 루프는 파일 docstring.
- **models/weights.py**(구 modules/weights ← load_pretrained — 유틸이라 상위 이동·소문자 유지, 임포트는 `import weights`): 가중치·에셋 취득 유틸 — StubUnpickler(피클 속 habitat Config 더미화), wp_pred/pretrain/finetuned 정리, load_partial(이름 매칭 관용), **load_compat**(정밀 로드 — Phase 7 zero-init 신규 키(frame_embed·node_flag_embed·node_type_embed)만 구 ckpt 결손 허용, 그 외 불일치는 오류; common.load_into·벤치 wp 로드가 사용). `BERT_CONFIG_JSON`/`BERT_VOCAB` = models/assets/ 평탄 파일.
- **modules/**: 태스크 모듈 패키지(내부는 상대 임포트, weights만 상위 절대 임포트; TRM은 pip `pytorch_transformers` 기반 — vendored 삭제). **backbones/** 2파일: `CrossModalTransformer.py`(구 NavCMT ← vilmodel_cmt + etp_ops + etp_transformer 병합, "# ==== 병합" 마커 — GlocalTextPathNavCMT 본체, 수정은 GraphPlanning 래퍼에서만), `DepthResNet.py`(구 ddppo_resnet — DD-PPO는 가중치 출처(habitat RL 사전학습)일 뿐 RL 코드 없음; SE/ResNeXt 변형 제거, F·Block 별칭 유지 주의 — F 누락으로 벤치 실행자 전멸 이력). 우리 코드는 `from backbones.x import ...`로만 접근. **assets/**: `bert_config.json`·`bert_vocab.txt` 평탄 2파일(하위 폴더 폐지). **원본 repo는 research_assets/ETPNav로 이동**(hw_vln_ce 컨테이너 /workspace/ETPNav — r2r_ce.py 폐루프만 사용).

## scripts/models/ (평탄화 — train/·test/ 폴더 폐지)

- **common.py**: 학습·평가 공용 — 해시 결정적 스플릿(게이트/궤적), freeze, AUC, ckpt 저장/로드, pick_gpu(0~3). **손실·지표 정의는 여기 한 곳**(학습 로그와 평가 보고가 같은 수를 말하도록): `heat_loss`(히트맵 BCE, HEAT_IGNORE=2 픽셀 제외), `heat_metrics`(**프레임별 계산 후 평균** — 배치 풀링과 값이 달라 혼용 금지: iou·prec·recall·auc·peak_hit·pos_rate), `heading_loss`(1−cos Δθ). 구 `wp_flat_target`(빈 CE 타깃)은 폐기.
- **train_traversability.py**(구 01_pretrain_passability): loss (a) g+GNN 사전학습 — 동결 트렁크 feature 1회 캐시(featcache) 후 GPU 상주, `--augment`(기본 1) 스텝별 그래프 증강. 출력 `/data/EVLN_ckpt/traversability.pt`. 현행 best AUC 0.8608.
- **train_waypoint.py**: 학습 2단계 = 제안기 히트맵 imitation. 입력 = depth **히스토리 K=4**(동결 인코더)+z, 손실 = `heat_loss` 단일(구 빈 CE·heading aux 폐기). **동결 정책**: g·urdf_enc를 `policy.freeze_stage1()`로 동결(네 소비처 공유라 손상 연쇄) → 학습 대상은 wp 본체 20.5M뿐. ckpt에는 동결분도 함께 저장(평가·다음 단계가 ckpt 하나로 복원). `--inject` 4변형. 지표 = IoU/prec/recall/auc/peak적중.
- **train_global.py**(신설): 학습 3단계 = 전역 선택기. **2단계 구조** — ① `--build-cache`로 동결 제안기를 1회 돌려 에피소드별 고스트(월드·feature·valid)+노드 임베딩 npz 캐시(에폭마다 재계산 금지) ② 학습은 캐시만 읽어 GraphMap을 홉 순서로 재생 → 선택기 forward(인코더·CLIP 미실행). 정답은 **위치로** 매칭(pose[t+1] 최근접 후보) — 저장된 gsel kind는 '현재 히트맵에 보였나'의 기록이지 후보 종류를 못박는 값이 아니다(이전 뷰의 살아있는 고스트가 정답인 경우가 흔함). **층 전환 홉**은 정답 좌표가 다음 층이라 현재 위치 최근접 전환 노드(계단·엘베)를 정답으로 삼고, 계단은 special 마커가 없어 포즈열 층 변화로 직전 노드를 계단 노드로 소급 표시. 손실 = 후보 CE + heading(1−cos Δθ, 정답 후보 위치에서만), z 토큰 첫 20% 선형 개방, 지시 없는 프레임은 무지시 토큰, 엘베 special 구간은 전역 미호출. 매칭 실패는 fwd/node로 분해 보고(제안기 품질 vs 기억 커버리지 진단).
- **eval.py**: 통합 평가 CLI — `python eval.py <sub>`: traversability/waypoint(**히트맵** 지표, 학습과 동일 입력=히스토리+depth 스템, 지표는 common 공용 함수)/rollout(폐루프 decision, `--drive` 실주행)/unseen/objreg. **objreg 함수화**(2026-07-27 수정): 모듈 레벨 스크립트로 남아 있어 어떤 서브커맨드를 돌려도 import 시점에 `CUDA_VISIBLE_DEVICES = sys.argv[1]`(=서브커맨드 이름)로 덮어써 GPU가 사라지고 GDINO까지 올라갔다 — 지금은 `main_objreg(--tag)`.
- **mansion_adapter.py**: 폐루프 부품 — UnionMemory(로봇별 topomap 합집합, `transition_nodes()` 추가), MemoryFeatures/GateBank(캐시; MemoryFeatures는 `push_history=False`로 오프라인 featurize — 구판은 에피소드 히스토리 버퍼를 오염시켰음), **판정과 실행 분리(2026-07-27)**: `plan_route`(다익스트라)는 **판정 전용**이고 실행 경로 호출 금지, 진입점은 `judge_state`(ok/notfound/embodiment) 하나로 통일 — 구판은 호출부마다 gate_sc·edge_block 조합이 달라(벤치 `{}`+edge_block / eval 판정 gate_sc+edge_block / eval drive 둘 다 없음) 같은 정책을 다른 조건으로 쟀음. **차단 규약 = z-차등 엣지 차단 하나**(`differential_block`): 노드 단위 `gate_sc`는 차등 가드가 없어 "전원 차단"(높이 협착)까지 세는데 그건 라벨엔 있고 expert 주행 GT(2D 폭)엔 없어 false_impossible을 만든다(GateBank가 전 게이트를 덮으므로 커버리지 손실 없음, gate_sc는 진단용으로만 잔존). **규약 통일로 eval rollout 수치가 바뀜(호텔, 같은 ckpt)**: zswap 상태 0.374→0.484 / 수단 0.927→0.943 / 갈림 재현 0.382→0.228 / conflict 0.842→0.836 / notfound AUC 0.875 동일 — 노드 차단이 만들던 false_impossible이 갈림으로도 집계되고 있었다는 뜻. 어차피 학습 4단계 후 재측정 대상이라 일관성을 우선함. **HopDriver**(신설) = 벤치·평가 공용 유일 주행 경로: 홉마다 observe→propose→lift→GraphMap.update→gmap_inputs→navigation 1회, 고스트 선택 시 `delete_ghost` 후 주행, 방문 노드는 기억 최단경로 백트래킹(실행 보조), 층 전환은 스킬 구간(선택 미호출; 엘베는 문 향한 도착 heading + 정지 대기, 계단은 등반), 홉 실패 시 그 후보 제외 후 재선택·3연속이면 stalled, 후보 0개여도 대체 후보 생성 안 함. `follow()`는 규칙 전역 ablation 집행(저수준 이동·스킬 공유). 주행 프리미티브 `avoid`/`move_action`(회전 선적용 보정, 막히면 None→홉 실패)도 여기로 통합. **ThorDriver**(층별 컨트롤러 풀 + 선제 재활용 RECYCLE_OPS=1200 + server_timeout=120 — 복구 try/except 없음, 실패는 감독자 소관; 층 로드 후 `SetObjectFilter([])`로 스텝당 오브젝트 메타데이터 직렬화 차단 — 호텔 실층 실측 292→167ms/장, 렌더 프레임 무영향·메타데이터 소비처 없음 확인 2026-07-27). datasets common은 `_mc()` 경로 로딩(`import common` 충돌 회피).
- (objreg는 eval.py 서브커맨드로 흡수)

## scripts/benchmark/ (2파일 + 유틸 2)

- **mansion.py**: MANSION 시뮬 벤치 — **감독자·실행자 구조**(2026-07-27 재설계, 예외처리 중첩 폐지): `--role super`(기본) = 작업 큐(에피소드 8개 유닛)·GPU 슬롯 배분·시차 기동(20s)·심박 감시(STALL_S 420s 정체 시 프로세스 그룹 kill 후 재큐잉, MAX_RETRY 3; 실행자는 에피소드 중 200스텝마다 심박 로그)·고아 THOR 청소(sweep_orphan_thor — 렌더러가 자체 세션이라 그룹 kill에 안 잡힘, 기동·킬마다 PPID=1 THOR 회수)·`--ep-per-unit`(유닛 크기 조절 — 잔여 적을 때 낮춰 병렬 폭 확보)·GPU 우선 인터리브 슬롯·완료 시 자동 집계 / `--role exec` = 선형 실행자(복구 없음, 항목별 장부 jsonl 즉시 기록+재개 스킵) / `--role agg` = 장부+레거시 샤드 json n-가중 병합 → mansion_sim_report.json / 도구 롤 validate·prep_gt·prep_robots(구 00_validate_sim·prep_real_gt·prep_real_robots 병합). **주행 = 홉 단위 전역 선택 루프로 교체(2026-07-27)**: 구 `run_episode`(규칙 경로 추종)·`WpDriver`(극좌표 빈 argmax) 폐지 — 극좌표 API(`N_DISTS`/`N_HEADINGS`/`candidate_to_pose`)가 모델 재작성으로 사라져 import부터 깨져 있었음. 실행은 `ma.HopDriver`가 전담하고 규칙 다익스트라는 상태 판정에만 남는다. `--variant learned`(기본, 학습 전역 선택기) / `rule`(규칙 전역 ablation — 저수준 이동·스킬은 learned와 공유해 차이를 전역 선택 하나로 좁힘). `--agent full|map` 폐지, `--ckpt03`(전역 선택기) 추가, ckpt 부재 시 경고 후 무작위 초기값으로 파이프라인만 구동(수치 무효). 목표형 지시를 `{tag}_instructions.json`에서 읽어 `policy('language')`로 주입(없으면 무지시 토큰) — 구판은 지시를 아예 안 넣었음. 판정 체계: 상태 축(불가능 미탐색/embodiment ↔ GT 대조, `judge_state`) + 실행 축(도착=STOP+ARRIVE_M 3.0m/arrival_error/stalled=연속 홉 실패 3/budget·max_hops 80). 구 wander 컷은 홉 상한이 대체(WANDER_S 제거). τ=3.0 고정, LiDAR 회피는 제어 층 전용, 노드 통과 1.2m, `--viz` 탑뷰 카드(GT/계획(규칙 팔만)/실주행+기억 그래프+범례). 장부 파일명은 태그 경계 매칭(hotel⊂elevonly 접두사 충돌 방지). **장부 키에 변형 포함(2026-07-27)**: 재개 스킵 키와 `ledger_rows` 중복 제거 키 둘 다 `(ep, rid, variant)` — 변형을 빼면 한 장부에 두 팔을 쌓을 때 먼저 쓰인 팔이 다른 팔의 같은 (ep,rid)를 가려 **뒤 팔이 통째로 건너뛰어지거나 집계에서 사라진다**(실측: 규칙 팔 549항목이 194로 집계됨). 자동 집계는 기본 `--legacy` 글롭이 구 벤치 디렉터리를 끌어오므로 파일럿처럼 격리된 측정에는 `--legacy ""`를 줄 것. **전량 측정 v2(2026-07-27, /data/EVLN_ckpt/benchmark_v2)**: 912항목 48분·재큐잉 1(THOR 카메라 무응답)·실패 유닛 0 — 주행 SR 0.897(n=662), 결과표는 check/result/ 표 6. 실행자 로그 파일명은 전체 태그 사용(구 tag[:24]는 hotel⊂elevonly 충돌로 로그 덮어씀 — 실패 사유 25건 유실), 장부 sr 항목에 reason 기록.
- **r2r_ce.py**: R2R-CE 벤치 — 기본 롤 = 폐루프(ETPNav 원본 파이프라인 @/workspace/ETPNav + Go2 embodiment 조건 + `--variant ours` g 게이팅, `--urdf-dir` 대조 실험, `--viz` 탑뷰 카드) / `--role report` = /data/R2R 서브셋 분리 리포트(MP3D 차단율·MANSION plan.success/refusal — 단일 SR 합산 금지) / `--role prep_frames` = R2R_go2 재렌더 덤프. hw_vln_ce 컨테이너에서 실행.
- **mansion_sim_env.py**(유틸): Gym류 시뮬 환경 — 텔레포트 스텝(회전 선적용), footprint 충돌(침식 그리드), 엘베 FSM(호출→대기→개문→탑승 적재검사→전환), 계단 GT 능력 강제. 로봇 폭별 존 anchor 재계산. **2026-07-27 규약 2건**: ① `dyaw`는 "이 이동의 도착 heading"이고 순수 회전(dx=dz=0, dyaw≠0)은 assert로 금지 — 엘베 호출 대기는 완전 정지(0,0,0)로 표현한다(구판은 문 앞 제자리 회전으로 FSM 호출 조건을 만족시켰음). ② `step(action, observe=False)` — 홉 루프는 홉 경계에서만 관측을 쓰므로 홉 내부 저수준 이동·스킬 구간의 렌더(스텝당 ~350ms)를 통째로 생략한다.
- **vln_metrics.py**(유틸): SR/SPL/NE/TL/nDTW + 다층 규약(층당 +10m, SR=동일층 3m) + Agg 집계기. 3방향 검증에서 수식 무죄 확인.

## check/benchmark/ (홉 루프 전환 검수, 2026-07-27)

- **p8_bench_rework.md** = 검수 카드(계약 스모크 10/10, 학습 팔 실주행, 규칙 팔, 재라벨, 판정 규약 통일 전후 수치), **hop_contract_smoke.log** = THOR 없이 observe→propose→lift→GraphMap→navigation→heading 계약 확인 로그.
- 홉 루프에서 발견해 고친 결함 3건: ① **제자리 홉**(도착 반경 1.2m 안 고스트를 골라 0스텝 "도착" → 홉 예산만 소모, stalled도 미발동. 고스트 도착 반경을 병합 반경 0.5m로 낮추고 순변위 `MIN_HOP_M=0.25m` 미만은 진행 불인정) ② **엘베 문 정렬**(순수 회전 금지 규약에서 존에 이미 들어와 있으면 문을 못 봐 FSM 호출 불가 → 문 쪽 0.2m 이동에 도착 heading을 실어 정렬) ③ **온라인 노드 행선**(이번 에피소드에 생긴 방문 노드는 uid 대응이 없어 무조건 홉 실패 → 저장 위치로 직행 분기, 현재 노드 재선택은 실패 처리로 재선택 유도).
- **게이트 재추출 → featcache 재생성 필수(2026-07-27 실측)**: 호텔 게이트를 4단 높이로 재추출하면서 스냅샷 행이 늘었는데(shard0 key 2268→7614행) `featcache/*.feat.npy`는 07-26판 그대로다. `GateBank`는 shard `key` 행과 featcache 행을 1:1로 짝지으므로 이 상태면 엉뚱한 feature를 쓰거나 IndexError가 난다 → **행 수 불일치 시 명시 예외**를 넣었다(조용한 오염 방지). 게이트를 다시 뽑았으면 `train_traversability` 캐싱 단계로 featcache를 다시 만들어야 벤치·평가가 그 환경을 쓸 수 있다. 재추출 안 된 3환경(office·townhouse·elevonly)은 07-26 featcache와 행 수가 일치해 그대로 사용 가능.
- **전량 파일럿(2026-07-27, `--variant rule` 3환경 549항목, `/data/EVLN_ckpt/benchmark_pilot`)**: 감독자 롤 완주 — 24유닛·실패 0·재큐잉 0, 재개 경로(잔여 183항목 이어받기)까지 확인. `[variant=rule] SR 0.941 SPL 0.880 NE 2.54 (n=322)`, elevator 태스크 1.000(n=26)·stairs 0.830(n=47). 같은 3환경 구 수치 0.919 → 0.941이고 증가분은 elevonly(0.896→0.955) — 낡은 중간층 노드·마지막 접근 수정이 층 전환 에피소드를 살린 결과와 일치. 학습 팔은 ckpt 미학습이라 SR 0.031로 무의미(파이프라인 구동 확인용).
- **미검증 인수인계**: `eval.py waypoint`는 loaders가 `heat`를 실어줘야 동작(없으면 KeyError로 명시 실패). 학습 팔 전량 측정은 학습 4단계 후.

## 벤치 결과 이력 (요약 — 상세 수치는 mansion_sim_report.json)

- **구 수치 재라벨(2026-07-27, 폐기 아님)**: 규칙 다익스트라 전역으로 측정한 기존 전량 수치(주행 SR 0.897 n=662 등)는 **"규칙 전역" ablation 베이스라인 행**으로 유지한다. 집계기가 변형 태그 없는 장부·레거시 샤드 json을 `variant=rule_legacy`로 자동 분류하고 `by=variant` 표를 추가로 낸다. 학습 전역(`variant=learned`) 재측정은 학습 4단계(제안기·전역 선택기) 완료 후 1회.
- 구판 무효 이력: wp ckpt 미로드 랜덤 헤드(전량 무효·삭제), r2r_val_unseen.json 스키마 항등값(삭제), τ−4 walk 차단 임의 규칙(삭제, G1 house 차단 원인), 노드 스킵/충돌60컷/wrong_floor·final_off(폐지·병합).
- v7 확정 체계 표본(limit3): 4환경 주행 SR 전부 1.000(LiDAR 회피 도입 후) — 시야-풋프린트 갭 stall 해소. 남은 축: 상태판정(기억 커버리지)·GT 불일치 도달(2D 폭 GT 보수성 — 본생성 전 검토 항목).
- THOR 웨지 대응: Unity 내부 결함이라 직접 수정 불가 — 선제 재활용+타임아웃(드라이버 수명 정책) + 감독자 재기동(구조적 처리). 커스텀 에셋 동시 로드 레이스 의심 → 시차 기동.
- **R2R-CE 전량 측정(2026-07-28, val_unseen 1,839ep 4샤드, `r2rce_{variant}_full_*.json`)**: base(Go2 조건) **SR 0.265** SPL 0.164 NE 8.06 TL 19.2 nDTW 0.335 oracle 0.388 / ours(g 게이팅 τ=−19.6) **SR 0.242** SPL 0.149 NE 8.17 oracle 0.351, 고스트 차단 36,055/324,744(11.1%). **게이팅이 성능을 깎는다** — MP3D 복도는 Go2(반경 0.155m)에게 사실상 전부 통과 가능하고 계단·엘베 갈림도 없어, 차단은 유효 후보만 제거하는 무작위 가지치기가 된다. 구 기록(base 0.125·ours 0.0)은 **에피소드 16개 표본**(샤드당 `--limit 4`)이었으므로 폐기하고 이 전량 수치를 쓸 것.
- **τ는 이식 금지, 분포에서 정할 것(2026-07-28 실측)**: g를 4단 게이트로 재학습한 뒤 ETPNav 파노라마 feature에서의 로짓 분포가 평균 **+3.7~6.6 → −8.96**(p1 −29.0 / p10 −19.6 / p50 −8.5 / p90 +1.5)으로 이동했다. 구 τ=2.5는 p90보다 위라 후보를 90% 넘게 차단 → 이것이 "ours 제로샷 0.0"의 원인이었다(시각 도메인 갭이 아니라 척도 이동). `attach_g_gate`가 `g_pct` 분위수를 남기도록 추가했으니 **τ는 매번 여기서 고를 것**. 차단 0%(τ=−1e9)면 base와 동일 동작(24ep 표본 SR 0.292)임을 확인.

## 리팩토링 기록 (2026-07-27, 사용자 지시 전면 재구성)

| 이전 | 이후 |
|---|---|
| /data/URDF/real_robots(v1)·real_robots_v2 | v1 삭제, v2 → **real_robots** |
| /data/EVLN_ckpt/passability.pt | **traversability.pt** |
| models/ETPNav/ (repo 클론) | **research_assets/ETPNav**(자산) + 필요 모듈만 models/에 이식 |
| models/pass_head.py | **traversability.py** (TraversabilityHead) |
| scripts/models/train/·test/ | 폐지 — **scripts/models/ 평탄화** (train_traversability·train_waypoint·eval·mansion_adapter·loaders·common) |
| data/loaders.py | **scripts/models/loaders.py** — data/는 심볼릭 앵커 전용 |
| models/ 평면 13파일 | **EA_Nav.py**(class EANav, 구 policy) + **modules/** 6파일 + **backbones/**(이식 백본) + **assets/**(bert_config), film→traversability 흡수, load_pretrained→weights |
| scripts/benchmark/ 8파일 | **mansion.py + r2r_ce.py** + 유틸 2(mansion_sim_env·vln_metrics). 00·01·02·03·04·prep_* 삭제(01_benchmark_mansion의 규칙 플래너 수치 0.752는 이력으로만 유지) |
| loaders.PassabilityDataset 등 | TraversabilityDataset·collate_traversability |

검증: models 로드 스모크(pretrain/finetuned+g+wp+tokenizer) 통과, 경로 스모크(common/adapter/_mc/loaders) 통과, mansion.py 감독자 실전(elevonly 잔여 유닛) 실행으로 기능 검증.

## docker/Dockerfile

- pybullet·yourdfpy 레이어 추가분. 컨테이너 numpy 2.4.6 vs Dockerfile `numpy<2` 불일치(기존 상태) — 재빌드 시 확인.

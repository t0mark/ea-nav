# 논문 초안 조각

## 모델 구조

### 설계 원칙

- 목표: embodiment 표현 z가 vision-language 판단을 변조 → 같은 지시·같은 환경에서 로봇 형태에 따라 다른 경로 산출
- 학습 대상 최소화: 학습되는 것은 ① URDF 그래프 인코더 ② embodiment-조건 스코어러뿐. 지도 작성·전역 플래닝·주행·grounding은 전부 무학습 규칙 → 행동 차이를 z의 기여로 깨끗하게 귀속 (ablation: z 제거/박스 치환/셔플/주입 방식 교체)

### 구성 요소

- **URDF 그래프 인코더 (학습 ①)**: 링크=노드(타입·primitive·치수·질량·관성), 조인트=양방향 엣지(타입·축·origin·리밋) → 엣지 조건 메시지 패싱 → z. 보조 프로브(외곽 치수 회귀 + 계단 가부)로 z가 이동 능력 정보를 담는지 검증
- **관측 인코더**: 전방 단일 뷰 RGB-D (파노라마 배제 — 제자리 회전 가정 제거, 부분 관측이 장기 기억의 필요성을 정당화). 사전학습 depth ResNet + 프로즌 CLIP, 동결
- **로컬 waypoint 스코어러 (학습 ②)**: transformer가 전방 각도 × 거리 × **도착 heading** 빈의 로짓 산출 — 전방 단일 뷰에서는 도착 방향이 다음 관측을 결정하므로 위치만으로는 불충분
- **장기 기억 (토폴로지 맵)**: rule-based 무학습. 노드=장소(포즈 + 방향별 스냅샷 + 시맨틱 임베딩), 엣지=주행 연결 + 층 전환. 잠재 목적지 레지스트리 — 관측했으나 미주행인 후보를 z로 판정하는 근거 제공
- **전역 플래너**: 순수 그래프 탐색. 층 전환 수단은 규칙(legged=계단 우선, wheeled=엘리베이터 전용) + 엘리베이터 대기 비용
- **goal grounding**: 프로즌 CLIP 텍스트↔기억 스냅샷 코사인 매칭 (무학습)
- **상태 출력**: 스코어 임계값 규칙 — 불가능(미탐색) / 불가능(embodiment) / 대기중(엘리베이터)

### z 주입: 하이브리드 2지점

- **통과성 스코어 헤드 g(후보 feature, z)**: "관측→통과성 스칼라" 단일 모듈이 3역할 겸무 — (i) waypoint 후보 로짓에 가산 바이어스(z 무시 지름길을 구조적으로 차단), (ii) 기억 스냅샷 랭킹·불가능 판정, (iii) 통과성 튜플 직접 사전학습 → 정책 학습으로의 전이 통로
- **embodiment 토큰**: z 사영 토큰을 지시 시퀀스에 삽입 → 지시×embodiment attention 상호작용(예: "계단으로 가" 지시와 wheeled의 충돌 해석). zero-init + 워밍업 스케줄로 사전학습 가중치 동작 보존
- 역할 분리 서사: g = "몸이 물리적으로 통과 가능한가"(지각-기하), 토큰 = "지시와 몸의 관계"(언어-embodiment). FiLM은 주입 방식 비교 ablation
- 사전학습 VLN 가중치(ETPNav 계열) 재활용 — 언어·시각 인코더 동결, 학습 파라미터는 전체의 소수

### 학습 절차

- 1단계: 통과성 튜플 (게이트 관측, z) → soft 라벨로 g·인코더 사전학습
- 2단계: embodiment별 expert 궤적로 waypoint imitation (+z 보조 프로브)

## 데이터셋

### 멀티플로어 데이터셋 생성

#### 배경 / 필요성

- embodiment에 따라 정답 행동이 갈리는 표본이 핵심: 같은 (시작, 목표)에서 로봇마다 경로·판정이 달라지는 divergence 쌍이 없으면 z를 무시하는 것이 최적해가 됨
- 다층 건물 씬(MansionWorld·AI2-THOR): 계단·엘리베이터 공존이 수단 선택 갈림을, 가구 협착부가 연속적인 폭 스펙트럼(클리어런스 사다리)을 자연 형성 — 씬 편집 불필요

#### 라벨 원천: 시뮬레이터가 아닌 씬 GT 기하

- 점유 그리드 + 로봇 반경 침식으로 통과성·경로를 직접 계산 (navmesh 불사용 → 로봇 반경이 연속 파라미터, 라벨과 expert 경로가 같은 기하 기준)
- 게이트 = 문 + 가구 협착부(스켈레톤 국소 폭 최소). 통과성 라벨 = (게이트 × 로봇) 행렬, 폭·높이 soft 마진 램프 — 관측은 게이트당 렌더, 라벨은 행렬로 곱셈적 생성

#### expert 궤적: 관측 기반 주행

- 전지적 최단 경로 금지 — 로봇은 건물 배치는 알되 "내 몸의 게이트 통과 가부"는 게이트가 카메라(FOV·거리 제한)에 잡힌 시점에 판정. 낙관적 주행 → 관측 후 우회 또는 포기
- 실패 시 부분 궤적 + 판정 지점 = 불가능(embodiment) 판정의 GT
- 층 전환: 계단 = 디딤판 등반 폴리라인(물리 통과), 엘리베이터 = 진입·내부 대기·진출 구간 → 대기중 상태 라벨이 궤적 위에 성립
- waypoint GT = 위치 + 도착 heading, 각 로봇 URDF 센서 실높이의 전방 RGB-D 프레임에 투영

#### 기억·지시·에피소드

- 토폴로지 기억: rule-based 탐사 궤적 → 노드 병합·루프 클로저, 방향별 스냅샷 + 방 앵커 360° 스캔. 프리픽스 절단으로 부분 기억 파생 — embodiment별 기억 차이(도달 불가 방 탈락, 전환 수단)가 자동 재현
- 지시 3형태 = LLM 직접 생성 + 역파싱 자동 검증(불일치 폐기): ① 목표형(goal GT 연동) ② R2R형(embodiment별 expert 경로 기준 — 같은 시작·목표에서 "take the elevator" vs "take the stairs"로 지시 자체가 갈림) ③ 결합형(부분 기억 + 탐사 중 실제 관측된 목표 → 기억 힌트 + 탐색 지시)
- 에피소드 배합: divergence 쌍 과표집 + 무갈림 45% 선행 제어, 에피소드×로봇 상태 GT(주행/대기 포함/사전 불가능/관측 후 불가능)
- 평가 전용 세트: z 스왑 통제(같은 기억·목표, z만 교체), 지시-충돌(다른 embodiment의 경로 지시), 미탐색 목표
- 전 라벨 자동 생성(사람 주석 없음), 시드 고정 결정적 재현

### 랜덤 URDF 생성 (embodiment 표본)

#### 배경 / 필요성

- URDF 인코더가 형태·기하 변화에 일반화되려면 학습 표본이 물리적으로 타당한 로봇 형태 분포를 넓게 덮어야 함
- 링크-관절 그래프의 무제약 샘플링 → 대부분 실재 불가능한 구조
- 실기 URDF 소수에 의존 → 형태 공간이 지나치게 희소
- → 실재 로봇 계열의 설계 규칙성을 사전 지식으로 삼는 절차적 생성 채택

#### 설계 원칙 3가지

- **문법으로 제약된 형태 공간**
    - 위상 구조는 계열별 문법(휴머노이드 / 다족 / 휠형)으로 고정
    - 무작위성은 문법 내부에만 부여: 이산 선택(다리 수·배치, 구동 방식, 관절 자유도, 센서 구성) + 연속 형상 파라미터
    - 대칭성 등 설계 규칙성이 문법에 내재 → 구조적으로 다양하되 실재 로봇 분포를 벗어나지 않음
- **치수 조건부 생성**
    - 외곽 치수 = 환경 통과 가능성을 지배하는 변수
    - 목표 치수를 먼저 지정하고 형태를 역으로 구성
    - 효과: 치수 공간 커버리지를 생성 단계에서 직접 통제, 능력이 갈리는 경계 영역에 표본 집중 가능
- **물리적 일관성**
    - 질량·관성은 기하와 밀도로부터 유도
    - 구동 한계는 지탱할 질량에 비례하도록 부여
    - 자기충돌 없음 + 정적 안정 + 목표 치수 부합 표본만 채택 (거부 샘플링)

#### 기타

- 센서(카메라·IMU·LiDAR 등)도 부착 위치를 갖는 구성 요소로 포함, 그래프 노드로 노출 → 인코더가 이동 형태뿐 아니라 감지 구성 차이까지 표현 가능
- 전 과정 결정적 재현 가능

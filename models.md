# 1. 모델 구조

**한 줄 요약**: "지형 파악 → 이 로봇이 갈 수 있는 곳 계산 → 경로 생성"의 3단계를 하나로 묶어 함께 학습하는 모델. 로봇의 생김새만 보고 그 로봇이 뭘 할 수 있는지 스스로 알아낸다.

## 입력과 출력

**입력** (추론할 때는 이게 전부, 정답 목적지 좌표는 안 줌):
- 카메라 영상 8프레임 (색+거리, 그 로봇의 카메라 높이에서 찍힌 것). 단순 시간 간격이 아니라 **포즈 기반 키프레임** — 일정 거리·회전각을 넘을 때만 새 프레임으로 채택
- 자연어 명령 (영어)
- 로봇 생김새 값: 반지름, 몸 높이, 다리 길이/바퀴 지름, 이동 방식(바퀴/네 발/두 발)
  - ※ "넘을 수 있는 턱 높이" 같은 능력치는 입력에 넣지 않음 (정답을 미리 알려주는 셈). 카메라 높이도 빼고 영상 만드는 데만 씀 (능력을 간접적으로 알려주는 힌트가 되므로)

**출력**:
- 위에서 내려다본 지도 위의 경유점 8개
- 갈 수 없으면 "도달 불가"라고 답하기
- 모터를 직접 움직이는 일은 모델 밖의 별도 제어기가 담당

## 모듈 4개

**① 지형 모듈** (로봇과 상관없는 부분)
- 영상을 이미지 신경망(DINOv2)으로 처리한 뒤, 위에서 내려다본 19m×19m 격자 지도로 변환
- 내놓는 것: 높이 지도 + 바닥 재질·사물 지도 + "실제로 본 곳이 어디인지" 표시
- 지나온 프레임을 쌓아 발밑과 뒤쪽을 보완. 인터넷 영상으로 미리 학습 가능

**② 로봇 인코더**
- 생김새 값을 작은 신경망에 넣어 하나의 특징 벡터로 만듦

**③ 통행 난이도 모듈** (로봇에 따라 달라지는 부분)
- 지형 지도 + 로봇 특징 → "이 로봇에게 각 칸이 얼마나 힘든가"를 0~1로 매긴 지도
- 지형 판단은 주변만 보면 되는 일이라 합성곱 신경망을 쓰고, 로봇 차이는 판단 기준 자체를 바꾸는 일이라 로봇 특징으로 신경망 출력을 조절하는 방식(FiLM)을 씀

**④ 경로 모듈**
- 명령문 + 영상 정보 + 난이도 지도를 신경망으로 합쳐 **거친 목표점 1개**를 예측
- 그 목표점을 향해 난이도 지도 위에서 경로를 찾는 층(Neural A* 또는 가치 반복 신경망)이 **경유점 8개를 산출** — 이 층 덕에 경로 오차가 앞 모듈까지 거슬러 전달됨
- 경로를 찾을 때 쓰는 목표점은 모델이 예측한 거친 목표점이지, 정답 목적지가 아님

## 학습 방법

**학습 신호 4개**: 경로 맞히기 + 난이도 지도 맞히기 + 지형 맞히기 + **짝 비교 학습**

**짝 비교 학습이 핵심**: 같은 장소·같은 명령을 로봇 두 종류로 동시에 통과시켜(같은 신경망 공유), "경로가 갈려야 하는 곳에서는 갈리고, 같아야 하는 곳에서는 같게" 직접 가르친다. 두 경로가 갈리는 정도에 비례해 더 세게. 한쪽이 아예 못 가는 짝은 "도달 불가" 판단으로 비교 학습.

**모듈 사이가 끊기지 않게**: 중간에서 값을 잘라내는 연산을 쓰지 않아 경로 오차가 ④→③→①까지 전달됨. 거친 목표점을 직접 맞히는 신호도 같이 줘서, 경로 찾기 층의 신호가 약해져도 학습이 멈추지 않게 함(보험).

**3단계로 학습**: ① 먼저 학습 → ②·③ 학습(로봇 인코더로 난이도 지도) → ④까지 붙여 전체 함께 미세조정. 정답 경로에서 일부러 벗어난 위치의 영상도 섞어 학습 — 안 그러면 한 번 경로를 벗어났을 때 못 돌아옴.

## 이 구조의 주장과 증명 계획

- 주장 1: 지형(공유)과 능력(로봇별)을 나눈 구조가 필요하다 → 난이도 지도를 빼고 로봇 정보를 경로 모듈에 바로 넣은 것과 비교
- 주장 2: 함께 학습하는 게 따로 학습보다 낫다 → 모듈별 따로 학습 후 연결한 것과 비교
- 주장 3: 짝 비교 학습이 효과 있다 → 그 신호를 뺀 것과 비교
- 주장 4: 생김새만으로 능력을 알아낼 수 있다 → 능력치를 미리 알려준 기준 모델과의 차이 측정
- 추가: 학습에 없던 로봇(생김새 값을 사이값으로)에 바로 적용해보기

## 주요 위험과 대비

1. **카메라가 낮은 로봇(네 발, 0.3m)은 앞쪽 바닥이 잘 안 보임** — 가장 위험. 대비: 안 본 곳은 "잘 모름"으로 표시해 그런 칸은 조심스럽게 판단 + 매 걸음 다시 계획. 그래도 안 되면 영상에서 바로 난이도를 매기는 방식과 섞는 쪽으로 전환(대비책 코드 미리 준비)
2. 경로 찾기 층까지 학습 신호가 너무 약해질 위험 — 경유점 직접 맞히기를 같이 둠. 그래도 안 되면 "중간 결과를 사람이 들여다볼 수 있는 분리 구조"로 설명 방향을 바꿈(이것도 그 자체로 강한 장점)
3. 통행 난이도 정답이 보행 제어기 성능에 좌우됨 — 제어기를 고정하고 그 사실을 명시, "고정 제어기 기준의 난이도"로 범위를 정직하게 좁힘
